# Environment Variables
# These files contain sensitive API keys and secrets. NEVER commit them.
.env
.env.*
backend/.env
frontend/.env

# Dependencies
# These folders can be regenerated by running `npm install`.
node_modules
backend/node_modules
frontend/node_modules

# Production Build
# This folder is generated when you build the React app.
frontend/build

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-generated files
.DS_Store
Thumbs.db